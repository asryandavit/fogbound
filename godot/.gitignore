# Godot 4 cache and import data — never commit
.godot/

# Android export — generated Gradle project + version marker (never hand-authored)
android/

# Export presets and exported builds
export_presets.cfg
*.apk
*.aab
*.ipa
*.dmg
*.exe
*.x86_64
*.app
